services:
  myclaw:
    build: .
    container_name: myclaw-agent
    env_file:
      - .env
    environment:
      PUBLIC_PORT: "80"
    ports:
      - "80:8080"
      - "8080:8080"
    restart: unless-stopped
